<h1>Movie Results</h1>

    <div class="movie-container">
      <table>
        <% @movies.each do |movie| %>
        <tr>
          <td><%=movie.title%></td>
          <td><%= link_to 'Show', movie_path(movie)%></td>
        </tr>
        <%end%>
      </table>

    </div>
